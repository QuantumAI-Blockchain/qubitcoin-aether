[package]
name = "aether-fabric"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Knowledge Fabric — sharded embedding store with HNSW vector search for Aether Mind V5"

[dependencies]
aether-types = { path = "../aether-types" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
log = "0.4"
parking_lot = "0.12"
sha2 = "0.10"
rand = "0.8"

[dev-dependencies]
approx = "0.5"
tempfile = "3"
