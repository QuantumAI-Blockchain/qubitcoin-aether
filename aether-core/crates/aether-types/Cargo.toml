[package]
name = "aether-types"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Shared types for the Aether Tree AI engine"

[dependencies]
pyo3 = { version = "0.23", features = ["auto-initialize"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
